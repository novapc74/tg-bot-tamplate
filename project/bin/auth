#!/usr/bin/env php
<?php

$password = 'sdfmlkk897()Gfweyxbnbn_%';
$hashed_password = password_hash($password, PASSWORD_DEFAULT);

if (password_verify($password, $hashed_password)) {
    echo PHP_EOL . "Password is valid!" . PHP_EOL;
}

echo $hashed_password . "\n\n";
